'use client';

import { CampaignForm } from '../components/local-forms';
import { SafetyNotice, CampaignCard, SectionHeader, XpeXPageShell } from '../components';
import { xpexCreatorCommerceMock } from '../mock-data';
import { useXpeXCommerceStore } from '../lib/use-xpex-commerce-store';

export default function XpeXCampaignsPage() { const { state, updateCampaignStatus } = useXpeXCommerceStore(); return <XpeXPageShell eyebrow="Campanhas · local" title="Campanhas operacionais locais" description="Planejamento manual com briefing salvo no localStorage; sem API, banco, Prisma ou automação."><SafetyNotice /><section className="rounded-[2rem] border border-white/10 bg-white/[0.055] p-7"><SectionHeader eyebrow="Funil" title="Produto → Radar IA → Criativo → Link → Postagem → Lead → Métrica → Otimização" /><div className="mt-6 grid gap-3 md:grid-cols-4">{xpexCreatorCommerceMock.funnel.map((step, index) => <div key={step} className="rounded-2xl border border-white/10 bg-black/20 p-4"><span className="text-xs font-black text-[#F5B301]">0{index + 1}</span><p className="mt-2 font-bold">{step}</p></div>)}</div></section><section className="grid gap-5 lg:grid-cols-2">{state.campaigns.map((campaign) => <div key={campaign.id} className="space-y-3"><CampaignCard campaign={campaign} /><select value={campaign.status} onChange={(event) => updateCampaignStatus(campaign.id, event.target.value as never)} className="w-full rounded-2xl border border-white/10 bg-black/40 px-4 py-3 text-sm text-white"><option>Planejado</option><option>Em validação</option><option>Em campanha</option><option>Vencedor</option><option>Pausado</option><option>Descartado</option></select></div>)}</section><CampaignForm /></XpeXPageShell>; }
